<div class="container">
  <h4>ジャンル編集</h4>
  <%= form_with model:[:admin,@genre] do |genre|%>
   <label for="name">ジャンル名</label>
   <%= genre.text_field:name %>
   <%= genre.submit'変更を保存'%>
  <% end %>
</div>