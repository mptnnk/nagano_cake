<div class="container">
 <h4>商品詳細</h4>
 <!--あとでスタイル-->
 <table class ="table">
   <tr>
     <th><%= image_tag @item.image,size:'250x250' %></th>
     <th><label for="name">商品名</label></th>
     <td><%= @item.name %></td>
     <th><label for="introduction">商品説明</label></th>
     <td><%= @item.introduction %></td>
     <td>
       <label for="genre">ジャンル</label><br>
       <%= @item.genre.name %>
     </td>
     <td>
       <label for="price">税込価格（税抜価格）</label><br>
       <%= @item.price %>
       <!--税込と税抜の表示は見本では改行されている。priceは税抜価格のため、後で税込価格の表示を作成する-->
     </td>
     <td>
       <label for="is_active">販売ステータス</label><br>
       <% if @item.is_active == true %>
        販売中
       <% else %>
        販売停止中
       <% end %>
     </td>
     <td>
       <%= link_to"編集する",edit_admin_item_path(@item.id) %>
     </td>
   </tr>
 </table>

</div>